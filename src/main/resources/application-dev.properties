# Development-only settings. Enabled when the "dev" profile is active.
# The H2 console is intentionally NOT enabled in the default/production config.
spring.h2.console.enabled=true
spring.jpa.show-sql=true
